#!/usr/bin/env python ./quiz.py
# Add two numbers so they add up to a number between 10 and 20
!random.sample(["%2d+%d= %s" % (x, y, x+y) for x in range(1, 20) for y in range (1, 20) if 10 <= (x + y) <= 20], 30)
